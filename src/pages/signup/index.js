// Library Imports
import React from "react";

// Relative Imports
import { Container } from "./styles";
const SignIn = ({ navigation }) => {
  return <Container />;
};

export default SignIn;
